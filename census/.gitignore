# Node deps for census-density.mjs (reinstall with `npm install`).
node_modules/

# Generated artifacts — not source.
census_data/
points.geojson
*.zip
# Cloud FlatGeobuf build output (~450 MB) — lives in GCS, never committed.
*.fgb
*.work.gpkg

# Python
__pycache__/
*.pyc
