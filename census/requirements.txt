# Python deps for sample_census.py (and download_census.py needs none — stdlib).
geopandas>=0.14
shapely>=2.0
scipy>=1.10
numpy>=1.24
pandas>=2.0
rasterio>=1.3   # optional: only to read the DEM bbox/CRS via --dem (skip with --bbox)
